const _table_name = {

}
const _status = {
    Active: "active",
    Inactive: "inactive",
}

const _envMode = {
    local: 'local',
    development: "development",
    production: "production",
    staging: "staging"
}

module.exports = {
    _table_name,
    _status,
    _envMode,
}